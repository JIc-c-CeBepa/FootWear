<UserControl x:Class="FootWear.UC.OrdersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FootWear.UC"
             mc:Ignorable="d" 
              d:DesignHeight="200" d:DesignWidth="1080" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock  Grid.Column="1" Text="{Binding DateDeliver, StringFormat=Дата доставки: {0}}" FontFamily="Times New Roman" TextWrapping="Wrap" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="25"/>

        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ArtikleList, StringFormat=Артикул заказа: {0}}" FontFamily="Times New Roman" TextWrapping="Wrap" FontSize="25"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Status, StringFormat=Статус заказа: {0}}" FontFamily="Times New Roman" FontSize="25"/>
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PickUpPointAdressNavigation.FullAddress,StringFormat=Адрес ПВ: {0}}" FontFamily="Times New Roman" FontSize="25"/>
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding DateStartOrder, StringFormat=Дата заказа: {0}}" FontFamily="Times New Roman" FontSize="25"/>
        </Grid>
     
    </Grid>
</UserControl>
